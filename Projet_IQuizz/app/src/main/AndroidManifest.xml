<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetcoo.projet_iquizz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.StatsActivity"/>
        <activity android:name=".controller.FinQuizz" />
        <activity android:name=".controller.Reprendre" />
        <activity android:name=".controller.Compte" />
        <activity android:name=".controller.QuestionActivity" />
        <activity android:name=".controller.ListeQuizz" />
        <activity android:name=".controller.ListeJoueurs" />
        <activity android:name=".controller.Game" />
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.NewCompte" />
    </application>

</manifest>